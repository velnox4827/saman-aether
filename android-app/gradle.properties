org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=false
android.nonTransitiveRClass=true

# AGP 9.x compatibility bridge.
# We keep the proven Kotlin Android plugin and current DSL for this release
# while modernizing AGP/Gradle/compileSdk in a controlled way.
android.builtInKotlin=false
android.newDsl=false
